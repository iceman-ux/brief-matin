feedparser>=6.0.11
PyYAML>=6.0
google-genai>=1.0.0
# Windows n'a pas de base de fuseaux horaires système, contrairement à
# Linux et macOS. Sans ça, ZoneInfo("Europe/Paris") échoue.
tzdata>=2024.1; platform_system == "Windows"
